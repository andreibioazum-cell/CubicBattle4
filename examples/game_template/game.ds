#include "player.ds"

num COLOR_BG = 0xFF20242B
num COLOR_PLAYER = 0xFFEE7722
num COLOR_JOY = 0xFFFFFFFF

fn init() {
    player_init()
}

fn update() {
    player_update()
}

fn draw() {
    cls(COLOR_BG)
    player_draw()
}

fn touch(x, y, action) {
    player_touch(x, y, action)
}
