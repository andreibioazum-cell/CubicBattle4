#ifndef DIMSCRIPT_COMPILER_H
#define DIMSCRIPT_COMPILER_H

/* The compiler emits code for the same runtime used by the Android host.
 * Keep this compatibility header as a single source of declarations instead
 * of maintaining a second, divergent copy of runtime.h. */
#include "runtime.h"

#endif
