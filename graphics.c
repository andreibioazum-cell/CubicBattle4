/*
 * Android's window-backed graphics implementation lives in main.c so the
 * current Buffer is available while ANativeWindow is locked.  This file is
 * kept as a source-compatible placeholder for build systems that enumerate
 * every C file; it intentionally contains no second set of graphics symbols.
 */
const int ds_graphics_translation_unit = 0;
