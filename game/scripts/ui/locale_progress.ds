public function tr_super()
    if language == 1 then
        return "Снежинка"
    end
    return "Snowflake"
end
public function tr_super_ebuc()
    if language == 1 then
        return "Ульта"
    end
    return "Ult"
end
public function tr_super_azum()
    if language == 1 then
        return "Рывок"
    end
    return "Dash"
end
public function tr_class_selected()
    if language == 1 then
        return "Выбран"
    end
    return "Selected"
end
public function tr_class_pick()
    if language == 1 then
        return "Выбрать"
    end
    return "Select"
end
public function tr_levels()
    if language == 1 then
        return "Уровни"
    end
    return "Levels"
end
public function tr_level()
    if language == 1 then
        return "Уровень"
    end
    return "Level"
end
public function tr_level_status_open()
    if language == 1 then
        return "Открыт"
    end
    return "Opened"
end
public function tr_level_status_locked()
    if language == 1 then
        return "Закрыт"
    end
    return "Locked"
end
public function tr_level_unlock_cost(number cost)
    local string out = ""
    if language == 1 then
        out = "Открыть · " + cost + " " + tr_cups_unit()
    else
        out = "Unlock · " + cost + " " + tr_cups_unit()
    end
    return out
end
public function tr_class_level_effect(number cls, number n)
    if cls == 1 && n == 1 then
        if language == 1 then
            return "Вторая жизнь: урон -10%"
        end
        return "Second life: damage -10%"
    end
    if cls == 1 && n == 2 then
        if language == 1 then
            return "Скорость +3%"
        end
        return "Speed +3%"
    end
    if cls == 1 && n == 3 then
        if language == 1 then
            return "Атака лечит +0.1 HP"
        end
        return "Attack heals +0.1 HP"
    end
    if cls == 2 && n == 1 then
        if language == 1 then
            return "Тик яда +0.15 урона"
        end
        return "Poison tick +0.15 damage"
    end
    if cls == 2 && n == 2 then
        if language == 1 then
            return "Тик яда +0.15 урона (x2)"
        end
        return "Poison tick +0.15 damage (x2)"
    end
    if cls == 2 && n == 3 then
        if language == 1 then
            return "Яд стакается, снежинка наносит урон"
        end
        return "Poison stacks, snowflake deals damage"
    end
    if cls == 3 && n == 1 then
        if language == 1 then
            return "Турель +2 HP"
        end
        return "Turret +2 HP"
    end
    if cls == 3 && n == 2 then
        if language == 1 then
            return "Спавн турели лечит +0.5 HP"
        end
        return "Placing a turret heals +0.5 HP"
    end
    if cls == 3 && n == 3 then
        if language == 1 then
            return "Луч турели лечит +0.1 HP/сек"
        end
        return "Turret beam heals +0.1 HP/sec"
    end
    if cls == 0 && n == 1 then
        if language == 1 then
            return "Урон атаки +5%"
        end
        return "Attack damage +5%"
    end
    if cls == 0 && n == 2 then
        if language == 1 then
            return "Скорость +2%"
        end
        return "Speed +2%"
    end
    if language == 1 then
        return "Атака лечит +0.05 HP"
    end
    return "Attack heals +0.05 HP"
end
public function tr_cups_unit()
    if language == 1 then
        return "кубков"
    end
    return "cups"
end
public function tr_candies_unit()
    if language == 1 then
        return "конфет"
    end
    return "candies"
end
public function tr_level_locked()
    if language == 1 then
        return "Купите предыдущий уровень"
    end
    return "Buy the previous level"
end
public function tr_level_need_cups()
    if language == 1 then
        return "Не хватает кубков для уровня"
    end
    return "Not enough cups for this level"
end
public function tr_levels_soon()
    if language == 1 then
        return "Скоро!"
    end
    return "Coming soon!"
end
public function tr_cups()
    if language == 1 then
        return "Кубки"
    end
    return "Cups"
end
public function tr_buy_for(number cost, string currency)
    local string out = ""
    if language == 1 then
        out = "" + cost + " " + currency
    else
        out = "" + cost + " " + currency
    end
    return out
end
public function tr_class_need_cups()
    if language == 1 then
        return "Не хватает кубков"
    end
    return "Not enough cups"
end
public function tr_class_need_candies()
    if language == 1 then
        return "Не хватает леденцов"
    end
    return "Not enough candies"
end
public function tr_skins()
    if language == 1 then
        return "Скины"
    end
    return "Skins"
end
public function tr_skin_normal()
    if language == 1 then
        return "Обычный"
    end
    return "Normal"
end
public function tr_skin_zombie()
    if language == 1 then
        return "Зомби"
    end
    return "Zombie"
end
public function tr_skin_legends_hint()
    if language == 1 then
        return "Легенды не умирают..."
    end
    return "Legends never die..."
end
public function tr_skins_need_legends()
    if language == 1 then
        return "Скин «Зомби»: достижение «Легенды не умирают...»"
    end
    return "Zombie skin: \"Legends never die...\" achievement"
end
public function tr_cups_reward(number amount)
    local string out = ""
    if language == 1 then
        out = "+" + amount + " кубков"
    else
        out = "+" + amount + " Cups"
    end
    return out
end
public function tr_cups_lose(number amount)
    local string out = ""
    if language == 1 then
        out = "" + amount + " кубков"
    else
        out = "" + amount + " Cups"
    end
    return out
end
public function tr_banned()
    if language == 1 then
        return "Вы забанены"
    end
    return "You are banned"
end
public function tr_promo()
    if language == 1 then
        return "Промокод"
    end
    return "Promo code"
end
public function tr_promo_title()
    if language == 1 then
        return "Промокод"
    end
    return "Promo code"
end
public function tr_promo_need_login()
    if language == 1 then
        return "Войди, чтобы получить свой код"
    end
    return "Log in to get your own code"
end
public function tr_promo_enter_ph()
    if language == 1 then
        return "Введи код с карточки..."
    end
    return "Enter the code from the card..."
end
public function tr_promo_submit()
    if language == 1 then
        return "Активировать"
    end
    return "Redeem"
end
public function tr_promo_how1()
    if language == 1 then
        return "Карточка выпадает в начале соло-боя, шанс 5%"
    end
    return "A card drops at the start of a solo battle, 5% chance"
end
public function tr_promo_need_card()
    if language == 1 then
        return "Код есть только на карточке из соло-боя"
    end
    return "The code is only on a card from a solo battle"
end
public function tr_promo_how2()
    if language == 1 then
        return "Код на ней случайный, награда одна на аккаунт"
    end
    return "Its code is random, one reward per account"
end
public function tr_promo_need_code()
    if language == 1 then
        return "Введи код"
    end
    return "Enter a code"
end
public function tr_promo_bad()
    if language == 1 then
        return "Неверный код"
    end
    return "Invalid code"
end
public function tr_promo_used_msg()
    if language == 1 then
        return "Промокод уже активирован"
    end
    return "Promo code already redeemed"
end
public function tr_promo_success()
    if language == 1 then
        return "+" + promo_reward_cups + " кубков и +" + promo_reward_candies + " леденцов!"
    end
    return "+" + promo_reward_cups + " cups and +" + promo_reward_candies + " candies!"
end
public function tr_card_title()
    if language == 1 then
        return "КАРТОЧКА"
    end
    return "CARD"
end
public function tr_card_sub()
    if language == 1 then
        return "Запомни код: больше он нигде не покажется"
    end
    return "Remember the code: it is not shown anywhere else"
end
public function tr_card_used1()
    if language == 1 then
        return "Награда за промокод уже получена"
    end
    return "The promo reward was already claimed"
end
public function tr_card_used2()
    if language == 1 then
        return "на этом аккаунте"
    end
    return "on this account"
end
public function tr_card_hint1()
    if language == 1 then
        return "Открой экран «Промокод» в меню"
    end
    return "Open the 'Promo code' screen in the menu"
end
public function tr_card_hint2()
    if language == 1 then
        return "и введи код, чтобы получить награду"
    end
    return "and enter the code to claim your reward"
end
