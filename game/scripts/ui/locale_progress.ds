function tr_super()
    if language == 1
        return "Снежинка"
    end
    return "Snowflake"
end
function tr_super_ebuc()
    if language == 1
        return "Ульта"
    end
    return "Ult"
end
function tr_super_azum()
    if language == 1
        return "Рывок"
    end
    return "Dash"
end
function tr_class_selected()
    if language == 1
        return "Выбран"
    end
    return "Selected"
end
function tr_class_pick()
    if language == 1
        return "Выбрать"
    end
    return "Select"
end
function tr_levels()
    if language == 1
        return "Уровни"
    end
    return "Levels"
end
function tr_level()
    if language == 1
        return "Уровень"
    end
    return "Level"
end
function tr_level_status_open()
    if language == 1
        return "Открыт"
    end
    return "Opened"
end
function tr_level_status_locked()
    if language == 1
        return "Закрыт"
    end
    return "Locked"
end
function tr_level_unlock_cost(number cost)
    string out = ""
    if language == 1
        out = "Открыть · " + cost + " " + tr_cups_unit()
    else
        out = "Unlock · " + cost + " " + tr_cups_unit()
    end
    return out
end
function tr_class_level_effect(number cls, number n)
    if cls == 1 && n == 1
        if language == 1
            return "Вторая жизнь: урон -10%"
        end
        return "Second life: damage -10%"
    end
    if cls == 1 && n == 2
        if language == 1
            return "Скорость +3%"
        end
        return "Speed +3%"
    end
    if cls == 1 && n == 3
        if language == 1
            return "Атака лечит +0.1 HP"
        end
        return "Attack heals +0.1 HP"
    end
    if cls == 2 && n == 1
        if language == 1
            return "Тик яда +0.15 урона"
        end
        return "Poison tick +0.15 damage"
    end
    if cls == 2 && n == 2
        if language == 1
            return "Тик яда +0.15 урона (x2)"
        end
        return "Poison tick +0.15 damage (x2)"
    end
    if cls == 2 && n == 3
        if language == 1
            return "Яд стакается, снежинка наносит урон"
        end
        return "Poison stacks, snowflake deals damage"
    end
    if cls == 3 && n == 1
        if language == 1
            return "Турель +2 HP"
        end
        return "Turret +2 HP"
    end
    if cls == 3 && n == 2
        if language == 1
            return "Спавн турели лечит +0.5 HP"
        end
        return "Placing a turret heals +0.5 HP"
    end
    if cls == 3 && n == 3
        if language == 1
            return "Луч турели лечит +0.1 HP/сек"
        end
        return "Turret beam heals +0.1 HP/sec"
    end
    if cls == 0 && n == 1
        if language == 1
            return "Урон атаки +5%"
        end
        return "Attack damage +5%"
    end
    if cls == 0 && n == 2
        if language == 1
            return "Скорость +2%"
        end
        return "Speed +2%"
    end
    if language == 1
        return "Атака лечит +0.05 HP"
    end
    return "Attack heals +0.05 HP"
end
function tr_cups_unit()
    if language == 1
        return "кубков"
    end
    return "cups"
end
function tr_candies_unit()
    if language == 1
        return "конфет"
    end
    return "candies"
end
function tr_level_locked()
    if language == 1
        return "Купите предыдущий уровень"
    end
    return "Buy the previous level"
end
function tr_level_need_cups()
    if language == 1
        return "Не хватает кубков для уровня"
    end
    return "Not enough cups for this level"
end
function tr_levels_soon()
    if language == 1
        return "Скоро!"
    end
    return "Coming soon!"
end
function tr_cups()
    if language == 1
        return "Кубки"
    end
    return "Cups"
end
function tr_buy_for(number cost, string currency)
    string out = ""
    if language == 1
        out = "" + cost + " " + currency
    else
        out = "" + cost + " " + currency
    end
    return out
end
function tr_class_need_cups()
    if language == 1
        return "Не хватает кубков"
    end
    return "Not enough cups"
end
function tr_class_need_candies()
    if language == 1
        return "Не хватает леденцов"
    end
    return "Not enough candies"
end
function tr_skins()
    if language == 1
        return "Скины"
    end
    return "Skins"
end
function tr_skin_normal()
    if language == 1
        return "Обычный"
    end
    return "Normal"
end
function tr_skin_zombie()
    if language == 1
        return "Зомби"
    end
    return "Zombie"
end
function tr_skin_legends_hint()
    if language == 1
        return "Легенды не умирают..."
    end
    return "Legends never die..."
end
function tr_skins_need_legends()
    if language == 1
        return "Скин «Зомби»: достижение «Легенды не умирают...»"
    end
    return "Zombie skin: \"Legends never die...\" achievement"
end
function tr_cups_reward(number amount)
    if language == 1
        return "+" + amount + " кубков"
    end
    return "+" + amount + " Cups"
end
function tr_banned()
    if language == 1
        return "Вы забанены"
    end
    return "You are banned"
end
function tr_promo()
    if language == 1
        return "Промокод"
    end
    return "Promo code"
end
function tr_promo_title()
    if language == 1
        return "Промокод"
    end
    return "Promo code"
end
function tr_promo_your_code()
    if language == 1
        return "Твой личный код (с карточки)"
    end
    return "Your personal code (from the card)"
end
function tr_promo_need_login()
    if language == 1
        return "Войди, чтобы получить свой код"
    end
    return "Log in to get your own code"
end
function tr_promo_enter_ph()
    if language == 1
        return "Введи код с карточки..."
    end
    return "Enter the code from the card..."
end
function tr_promo_submit()
    if language == 1
        return "Активировать"
    end
    return "Redeem"
end
function tr_promo_how1()
    if language == 1
        return "Карточка выпадает в соло с шансом 1/100"
    end
    return "Cards drop in solo with a 1/100 chance"
end
function tr_promo_how2()
    if language == 1
        return "+1% за матч без карточки (до 20%), награда одна"
    end
    return "+1% per match without a card (up to 20%), one-time"
end
function tr_promo_need_code()
    if language == 1
        return "Введи код"
    end
    return "Enter a code"
end
function tr_promo_bad()
    if language == 1
        return "Неверный код"
    end
    return "Invalid code"
end
function tr_promo_used_msg()
    if language == 1
        return "Промокод уже активирован"
    end
    return "Promo code already redeemed"
end
function tr_promo_success()
    if language == 1
        return "+" + promo_reward_cups + " кубков и +" + promo_reward_candies + " леденцов!"
    end
    return "+" + promo_reward_cups + " cups and +" + promo_reward_candies + " candies!"
end
function promo_used_state()
    if net_promo_used() == 1
        if language == 1
            return "Активирован"
        end
        return "Redeemed"
    end
    if language == 1
        return "Не активирован"
    end
    return "Not redeemed yet"
end
function tr_card_title()
    if language == 1
        return "КАРТОЧКА"
    end
    return "CARD"
end
function tr_card_sub()
    if language == 1
        return "Твой личный промокод"
    end
    return "Your personal promo code"
end
function tr_card_need_login()
    if language == 1
        return "Войди в аккаунт, чтобы получить свой код"
    end
    return "Log in to get your personal code"
end
function tr_card_hint1()
    if language == 1
        return "Открой экран «Промокод» в меню"
    end
    return "Open the 'Promo code' screen in the menu"
end
function tr_card_hint2()
    if language == 1
        return "и введи код, чтобы получить награду"
    end
    return "and enter the code to claim your reward"
end
