include graphics
function fit_w(number w)
    return clamp(w, 0, screen_w - 2 * screen_margin)
end
function center_x(number w)
    return (screen_w - w) / 2
end
function title_y()
    return back_y + btn_h + title_gap
end
function content_top()
    return title_y() + list_top_gap
end
function menu_x()
    return center_x(btn_w)
end
function menu_stride()
    return btn_h + btn_gap
end
function menu_top_for(number rows)
    return screen_h / 2 - (rows * btn_h + (rows - 1) * btn_gap) / 2
end
// Меню «ещё»: четыре строки (настройки, аккаунт, достижения, задания).
function more_row(number i)
    return menu_top_for(more_rows) + i * menu_stride()
end
function hit_more_row(number px, number py, number i)
    return hit_rect_pad(px, py, menu_x(), more_row(i), btn_w, btn_h)
end
// Настройки: 5 строк — язык, музыка (− / + по половинам), хитбоксы, FPS,
// апскейл. Центрируем отдельно, чтобы на низких экранах строки не уходили
// за нижний край.
number SETTINGS_ROWS=5
function settings_top()
    number top = screen_h / 2 - (SETTINGS_ROWS * btn_h + (SETTINGS_ROWS - 1) * btn_gap) / 2
    if top < 8
        top = 8
    end
    return top
end
function settings_row_y(number i)
    return settings_top() + i * menu_stride()
end
function hit_settings_row(number px, number py, number i)
    return hit_rect_pad(px, py, menu_x(), settings_row_y(i), btn_w, btn_h)
end
function hit_settings_music_minus(number px, number py)
    return hit_rect_pad(px, py, music_minus_x(), settings_row_y(1), music_half_w(), btn_h)
end
function hit_settings_music_plus(number px, number py)
    return hit_rect_pad(px, py, music_plus_x(), settings_row_y(1), music_half_w(), btn_h)
end
function music_half_w()
    return (btn_w - music_btn_gap) / 2
end
function music_minus_x()
    return menu_x()
end
function music_plus_x()
    return menu_x() + btn_w - music_half_w()
end
function lobby_play_x()
    return center_x(lobby_play_w)
end
function lobby_play_y()
    number block = lobby_play_h + lobby_gap + lobby_sub_h + lobby_gap + lobby_sub_h
    return screen_h / 2 - block / 2
end
function lobby_sub_x()
    return center_x(lobby_sub_w)
end
function lobby_sub_y(number i)
    return lobby_play_y() + lobby_play_h + lobby_gap + i * (lobby_sub_h + lobby_gap)
end
function hit_lobby_play(number px, number py)
    return hit_rect_pad(px, py, lobby_play_x(), lobby_play_y(), lobby_play_w, lobby_play_h)
end
function hit_lobby_sub(number px, number py, number i)
    return hit_rect_pad(px, py, lobby_sub_x(), lobby_sub_y(i), lobby_sub_w, lobby_sub_h)
end
function lobby_title_y()
    return lobby_play_y() - 64
end
function lobby_hello_y()
    return 18
end
function lobby_money_y()
    return lobby_sub_y(1) + lobby_sub_h + 16
end
function lobby_msg_y()
    return lobby_money_y() + money_line_h + 6
end
function hit_chat_circle(number px, number py)
    return hit_rect_pad(px, py, chat_cx - chat_r, chat_cy - chat_r, chat_r * 2, chat_r * 2)
end
function classes_card_w()
    number n = classes_visible_count()
    if n < 1
        n = 1
    end
    number total_gap = class_gap * (n - 1)
    number w = (screen_w - 2 * screen_margin - total_gap) / n
    return clamp(w, class_card_min_w, class_card_max_w)
end
function classes_total_w()
    number n = classes_visible_count()
    if n < 1
        n = 1
    end
    return classes_card_w() * n + class_gap * (n - 1)
end
function classes_top()
    return back_y + btn_h + title_gap
end
function classes_card_x(number vi)
    return center_x(classes_total_w()) + vi * (classes_card_w() + class_gap)
end
function class_sprite_scale()
    return 1.55
end
function class_sprite_y()
    return classes_top() + 12
end
function class_title_scale()
    return 0.85
end
function class_title_y()
    return classes_top() + 82
end
function class_desc_y()
    return classes_top() + 114
end
function class_level_y()
    return classes_top() + 136
end
function class_pick_inset()
    return 24
end
function class_pick_w()
    return classes_card_w() - 2 * class_pick_inset()
end
function class_pick_y()
    return classes_top() + class_card_h - class_pick_h - class_pick_pad
end
function hit_class_card(number px, number py, number vi)
    return hit_rect(px, py, classes_card_x(vi), classes_top(), classes_card_w(), class_card_h)
end
function class_actions_y()
    return classes_top() + class_card_h + class_gap
end
function class_action_w()
    number half = (screen_w - 2 * screen_margin - class_action_gap) / 2
    return clamp(half, 0, btn_w)
end
function levels_btn_x()
    if player_class != CLASS_AZUM
        return screen_w / 2 - class_action_w() / 2
    end
    return screen_w / 2 - class_action_gap / 2 - class_action_w()
end
function skins_btn_x()
    return screen_w / 2 + class_action_gap / 2
end
function class_has_skins(number cls)
    if cls == CLASS_AZUM
        return 1
    end
    return 0
end
function skins_btn_visible()
    return class_has_skins(player_class)
end
function hit_levels_btn(number px, number py)
    return hit_rect_pad(px, py, levels_btn_x(), class_actions_y(), class_action_w(), btn_h)
end
function hit_skins_btn(number px, number py)
    if skins_btn_visible() == 0
        return 0
    end
    return hit_rect_pad(px, py, skins_btn_x(), class_actions_y(), class_action_w(), btn_h)
end
function class_msg_y()
    return class_actions_y() + btn_h + 14
end
function stat_bar_w()
    return fit_w(520)
end
function stat_bar_h()
    return 26
end
function stat_hp_y()
    return screen_h - 110
end
function stat_power_y()
    return screen_h - 46
end
function stat_motto_y()
    return stat_hp_y() - 72
end
function levels_w()
    return fit_w(list_w)
end
function levels_x()
    return center_x(levels_w())
end
function levels_top()
    return content_top() + 30
end
function levels_row_y(number n)
    return levels_top() + (n - 1) * (level_row_h + level_row_gap)
end
function levels_msg_y()
    return levels_row_y(max_level) + level_row_h + 12
end
function hit_level_row(number px, number py, number n)
    return hit_rect(px, py, levels_x(), levels_row_y(n), levels_w(), level_row_h)
end
function skins_total_w()
    return skin_card_w * SKIN_COUNT + skin_card_gap * (SKIN_COUNT - 1)
end
function skins_card_x(number i)
    return center_x(skins_total_w()) + i * (skin_card_w + skin_card_gap)
end
function skins_top()
    return content_top() + 30
end
function skin_preview_scale()
    return 1.8
end
function skin_preview_y()
    return skins_top() + 16
end
function skin_name_y()
    return skins_top() + 118
end
function skin_status_y()
    return skins_top() + 146
end
function skins_msg_y()
    return skins_top() + skin_card_h + 16
end
function hit_skin_card(number px, number py, number i)
    return hit_rect(px, py, skins_card_x(i), skins_top(), skin_card_w, skin_card_h)
end
function ach_w()
    return fit_w(760)
end
function ach_x()
    return center_x(ach_w())
end
function ach_top()
    return content_top()
end
function ach_card_y(number i)
    return ach_top() + i * (ach_card_h + ach_card_gap)
end
function lb_w()
    return fit_w(lb_w_max)
end
function lb_x()
    return center_x(lb_w())
end
function lb_top()
    return content_top()
end
function lb_row_y(number i)
    return lb_top() + i * (lb_row_h + lb_row_gap)
end
function login_x()
    return center_x(login_w) + login_ox
end
function login_nick_y()
    return screen_h / 2 - 100 + login_oy
end
function login_pass_y()
    return login_nick_y() + login_fh + login_field_gap
end
function login_btn_y()
    return login_pass_y() + login_fh + login_btn_gap
end
function login_btn_x()
    return center_x(login_btn_w)
end
function login_msg_y()
    return login_btn_y() + btn_h + 16
end
function hit_login_field(number px, number py, number field_y)
    return hit_rect(px, py, login_x(), field_y, login_w, login_fh)
end
function hit_login_btn(number px, number py)
    return hit_rect_pad(px, py, login_btn_x(), login_btn_y(), login_btn_w, btn_h)
end
function modes_top()
    return screen_h / 2 - (modes_rows * btn_h + (modes_rows - 1) * btn_gap) / 2
end
function modes_row(number i)
    return modes_top() + i * menu_stride()
end
function hit_modes_row(number px, number py, number i)
    return hit_rect_pad(px, py, menu_x(), modes_row(i), btn_w, btn_h)
end
function quest_w()
    return fit_w(760)
end
function quest_x()
    return center_x(quest_w())
end
function quest_top()
    return content_top()
end
function quest_row_y(number i)
    return quest_top() + i * (ach_card_h + ach_card_gap)
end
function hit_quest_row(number px, number py, number i)
    return hit_rect(px, py, quest_x(), quest_row_y(i), quest_w(), ach_card_h)
end
